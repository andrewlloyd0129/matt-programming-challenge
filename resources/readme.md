To build this stuff go to the root directory and run
 mvn package

To run the program, run this in the root directory
 java -cp target/programming-challenge-1.0-SNAPSHOT.jar main.App < resources/input.csv